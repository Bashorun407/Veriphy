spring.application.name=verifhy

#USING HARD-CODED CREDENTIALS
#spring.datasource.url=jdbc:postgresql://localhost:5432/document_db
#spring.datasource.username=postgres
#spring.datasource.password=1234qwer

#spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true

# =======================
#   FLYWAY CONFIG
# =======================
#spring.flyway.enabled=true
#spring.flyway.baseline-on-migrate=true
#spring.flyway.locations=classpath:db/migration

# Datasource - use environment variables if present
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/document_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234qwer}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Flyway config
spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:true}
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Server port
server.port=${PORT:8080}


